# AGENTS.md

## Project brief

This repository contains a Pi-powered Microsoft Office add-in stack centered on an independent shared taskpane for Word, Excel, and PowerPoint. Companion-era archive code is not part of the active runtime and should not be reintroduced into tracked source.

## Product goal

Pi-Office aims to be a public source-available, state-of-the-art AI assistant for Microsoft Word, Excel, and PowerPoint that gives individuals and teams professional document-generation and editing help without forcing them into a single expensive enterprise subscription. The thesis is that the core capability should come from whichever AI subscription or inference provider a user already has access to, while the add-in supplies the Office-native UX, tool orchestration, privacy posture, and document-editing taste layer.

The product should be provider-flexible rather than model-vendor-bound. It should support major AI runtimes and auth patterns where feasible, including Pi via `pi-mono`, GitHub Copilot OAuth-style access, ChatGPT tiers with Codex-style agent capability, Anthropic-compatible paths where officially permitted, OpenCode offerings, OpenRouter, Cloudflare, Vercel, and future providers with comparable inference/tool APIs. Provider support must be honest about capabilities, restrictions, and token/auth state.

The competitive context includes Claude's Office add-in, ChatGPT/Codex-style add-ins, Microsoft Copilot, and other Office AI assistants. Those products can be powerful and well integrated, but they may be costly, subscription-tied, telemetry-opaque, access-restricted, uneven across document tasks, or weaker than desired for professional editing UX and user-specific writing taste. Pi-Office should compete by combining transparent local/Office behavior, broad provider choice, excellent Office.js leverage, LLM tool calling, optional visual reasoning for diagrams/images/layout, and a polished workflow for research papers, pitch decks, resumes, specs, business user stories, spreadsheets, DCF analysis, and similar high-value professional artifacts.

The core product bar is not just "chat in a taskpane." It is an interactive, trustworthy Office workbench: native Office edits where possible, strong context gathering, reviewable proposed changes, privacy-conscious connector behavior, clear auth/telemetry disclosure, saved-vs-unsaved document safety boundaries, and a UX that helps users create high-quality documents with less friction and more taste.

## Originality and provenance policy

- Office.js is the public platform foundation. Build on Microsoft-documented APIs, web standards, and original Pi-Office code.
- Competitor add-ins and local captures may be studied for capability mapping, user expectations, and gap analysis only. Do not copy their source code, assets, prompts, UI text, distinctive interaction patterns, private API contracts, or bundled resources.
- When a competitor-inspired capability is worth building, re-spec it in Pi-Office terms: user problem, public API basis, original UX, original implementation approach, and validation evidence.
- Keep a provenance trail for risky inspiration work in `docs/provenance.md` and `backlog.md` before public release.

## Companion architecture direction

- Basic mode is taskpane-only: the add-in owns the in-browser Pi runtime, provider configuration, chat loop, and Office.js tool execution.
- Advanced mode is add-in plus companion: the companion should own inference, provider auth/session state, MCP, memory, non-Office tools, tool calling, and local workspace context; the taskpane should become the presentation layer plus structured Office.js executor.
- Office tools stay in the add-in because only the Office host can safely and directly execute Office.js against the active document.
- Basic-to-advanced migration must preserve user settings, preferences, enabled providers/models, and connector configuration. Secret migration must be explicit and should prefer companion/OS keychain storage when available.
- Companion filesystem access defaults to read-only saved-document/workspace context. Shell access must not be exposed as raw host bash; if added, it must route through a tested read-only sandbox with writable scratch only, denied secret patterns, no network by default, and fail-closed approvals.

## Fresh session startup

- At the start of every fresh session, read `AGENTS.md` and `backlog.md` before substantial investigation, planning, or implementation.
- Correlate new user requests against existing `backlog.md` task IDs so related discussion, fixes, and commits stay tied to the running backlog.
- Use `backlog.md` to help guide the user toward sensible next work when they ask for direction, when a request is ambiguous, or when an existing backlog item is clearly relevant.

## Running project memory

- Maintain this file as durable project memory. Agents must keep the `Project summary` and `Last worked on` sections current without requiring explicit approval or discussion with the user.
- Update `Project summary` when architectural direction, committed state, major features, validation posture, repo workflow, or backlog ownership changes.
- Update `Last worked on` whenever an agent finishes meaningful investigation, planning, implementation, review, validation, commit, or handoff work.
- Keep these notes concise but specific enough that a future agent can resume without needing prior chat context.

## Project summary

- Pi-Office is moving toward an independent shared Office taskpane for Word, Excel, and PowerPoint, with Pi as the external AI runtime and Office host behavior kept separate from Pi auth/runtime concerns.
- The durable product goal is a public source-available, provider-flexible, privacy-conscious Office AI workbench that can use a user's existing AI subscription/provider access instead of locking them into one expensive enterprise assistant.
- Public distribution is now source-available under the `Pi-Office Personal Non-Commercial License v1.0`: personal/evaluation forks are allowed with attribution to `https://github.com/cryptekbits/Pi-Office`, while commercial use requires prior written permission.
- Public repo governance now relies on `.github/CODEOWNERS` plus a GitHub `Protect main` ruleset: `main` requires PR review, code-owner approval, stale-review dismissal, last-push approval, conversation resolution, and blocks force pushes/deletion; direct-push bypass is limited to the repository-admin role because personal GitHub repos cannot target named-user bypass actors.
- CI now runs on Node 24 with `actions/checkout@v6` and `actions/setup-node@v6`, matching GitHub's Node 24 action-runtime transition and the repository's Node 20+ engine floor.
- The originality policy is idea-level inspiration only: competitor captures can guide capability analysis, but Pi-Office must use original code, prompts, UI text, assets, and public API implementations.
- Advanced companion direction is now explicit: companion-owned inference/providers/MCP/non-Office tools, with taskpane-owned Office.js execution and seamless settings migration from taskpane-only mode.
- The active add-in surface now lives under `addin/`: `addin/apps/taskpane`, `addin/packages/pi-office-pack`, `addin/manifests`, and `addin/scripts`. The optional companion is split into the top-level `companion/` package; old `archive/companion` source and `.factory` scaffolding have been purged from tracked history and should not be restored.
- The repo root is a lightweight command router. Add-in dependencies and lockfile live in `addin/`; companion dependencies and lockfile live in `companion/`; there is no Turbo/Nx workspace layer.
- The repo now has a canonical `backlog.md` for review findings, feature gaps, bugs, improvements, security items, and testing work, with IDs intended to be referenced in commits and future implementation.
- The 2026-04-26 implementation review identified release-blocking "capability honesty" work around OAuth, remote connectors, provider readiness, raw Office.js execution, permission prompts, visual capture fidelity, and privacy/storage disclosure.
- Current hardening makes tool permissions fail closed on timeout, makes `office_execute_js` a manual-only escape hatch category, prevents connector imports/OAuth completion from creating false connected OAuth state, and routes verified read-safe local stdio plus remote HTTP MCP connectors through the optional companion.
- `SECURITY-001` is closed: connector OAuth completion now requires a verified credential handoff, manual UI completion is removed, tokenless/imported OAuth records report `auth_required`, and callback tests cover no-token/cancelled/mismatch/expired/success/import paths.
- `SECURITY-002` is closed: timeout, disconnect, and session cleanup paths now have automated fail-closed permission coverage across write-doc, connector, read-external, and write-external tool categories.
- `SECURITY-003` is closed: `office_execute_js` is an escape-hatch tool that cannot be auto-approved and any approval is normalized to a one-time decision, even if a client sends a broader scope.
- `SECURITY-005` is closed with an expanded `docs/provenance.md`, new `CONTRIBUTING.md` originality guidance, and a neutralized CSS comment; `SECURITY-007` is obsolete because the stakeholder accepted connector logos as-is without a licensing audit.
- `SECURITY-006` is closed: the companion now has shared shell capability protocol, fail-closed backend detection, policy validation, destructive probes, environment scrubbing, output caps/timeouts, a sandbox-routed Pi `BashOperations` adapter, and taskpane gating so `bash` only appears when a saved-document companion session reports shell `available`.
- `SECURITY-004` is closed: the taskpane now declares an Office-compatible CSP meta policy, Settings includes Privacy disclosure and clear-data controls, provider/connector clear-all routes remove stored encrypted envelopes plus local crypto keys, saved chat history can be cleared, and privacy/storage behavior is documented.
- `BUG-010` is closed: add-in clean install now reports zero npm audit vulnerabilities, Vite is patched to `8.0.10`, Mermaid renders through a lazy-loaded chunk, and the build no longer emits the large-chunk warning while the custom bundle-budget gate remains active.
- `BUG-005` is closed: provider/model readiness now separates stored credentials from verified usable credentials, labels unverified/auth-failed states in Settings and model selection, migrates old stored keys to unverified, promotes providers after successful use, and demotes 401/403/auth failures.
- `BUG-003` is closed: local stdio connector launches now merge safe inherited env, connector env, and the intended manual/env/detected credential variable; missing credential values or manual secrets without env targets stay auth-required without leaking secret values.
- `BUG-008` is closed: Excel rewind restores captured formula matrices instead of flattening formulas to values, falls back with warnings only when formula data is unavailable, and surfaces the current checkpoint fidelity boundary.
- `BUG-007` is closed: visual capture contracts now describe Office.js context/active-selection snapshots instead of OS screenshots or arbitrary offscreen Excel range renders, and `read_range_image` fails with selection guidance when the requested range does not match the active Excel selection.
- `BUG-009` is closed: PowerPoint selected-shape anchors now use each shape's owning parent slide when Office.js exposes it, and the legacy `edit_slide_master` tool is clearly apply-existing-layout only rather than slide-master mutation.
- `BUG-004` is closed: Office state refreshes are latest-only per session, repeated transient refresh failures are deduped, automated gates passed, and Word desktop smoke confirmed load/reconnect, rapid selection refresh, focus, streaming scroll, and non-mutating review behavior.
- `BUG-011` is closed: companion discovery now supports loopback CORS from the taskpane origin, Integrations diagnostics can show companion machine runtime checks, connector changes resync the active session tool inventory, and verified remote HTTP MCP connectors route through the companion instead of remaining browser setup-only.
- `BUG-012` is closed: opening the dev taskpane directly in a browser now enters an honest browser preview mode with a synthetic unsaved Office context, query-string host chrome selection, clear no-Office-host messaging, and Office document tools hidden from preview model sessions.
- `BUG-013` is closed: hosted HTTP connector setup no longer shows local companion warning copy, OAuth sign-in opens from the user-initiated UI click path in a dedicated named window, and the runtime now only generates OAuth URLs/state rather than opening windows itself.
- `BUG-015` is closed: hosted HTTP connector setup is no longer disrupted by companion polling, companion diagnostics stay in background state instead of rebuilding the active wizard draft, Slack MCP is planned/disabled until a registered Slack app path exists, Granola browser-side OAuth failures get broker/companion guidance, and local HTTP/STDIO profiles remain companion-routed.
- `BUG-014` is closed by stakeholder decision: the main taskpane JS bundle budget is now 3 MB to accommodate the richer connector catalog while keeping the single-chunk cap intact; `check:bundle` passes again with the current build.
- `BUG-018` is closed: Parallel Search MCP now uses current first-party endpoints (`https://search.parallel.ai/mcp` and `/mcp-oauth`), OAuth discovery routes to `platform.parallel.ai`, and the wizard saves the selected OAuth draft before starting sign-in so existing free-search records do not cause stale non-OAuth errors.
- `BUG-017` is closed: companion-brokered OAuth callback completion now syncs back into taskpane connector state, treats companion-held tokens as sufficient without copying them into taskpane storage, polls pending OAuth status in Integrations, and re-verifies the connector after callback completion.
- `FEATURE-002` has its first implementation slice: provider catalog entries now carry support status, auth methods, runtime surface, browser-callable state, companion requirement, subscription-backed state, and image support. Browser taskpane API-key providers remain executable, while Codex/Copilot/Gemini CLI/Antigravity/Bedrock/Vertex/Azure-style paths are visible as planned companion/OAuth or companion-auth work instead of fake browser API-key setup.
- `FEATURE-002` now also has provider/model curation metadata from canonical `provider-model-preferences.yaml`: Simple/Advanced settings visibility, lab/family labels, recommended/default models, un-recommended warning gates, user default model preferences, and Pi catalog drift notes validated against refreshed `pi-mono` commit `05f79b08` / `@mariozechner/pi-ai@0.70.2`.
- `IMPROVEMENT-005` is closed: Simple settings show curated recommended provider/model choices, Advanced settings expose the full Pi catalog, direct regional providers such as DeepSeek/Z.AI/Kimi Coding stay Advanced-only, and selecting un-recommended enabled models requires confirmation unless the user suppresses the warning.
- `BUG-006` is closed: image generation is explicitly OpenAI-only in the browser taskpane, Settings no longer advertises Google/OpenRouter image setup, `/v1/image-models` remains OpenAI-only, and unsupported image model preference writes fail during configuration.
- `FEATURE-003` is closed: the Office pack now ships typed professional workflow packs for Word, Excel, and PowerPoint, injects them into prompt/skill guidance, and exposes host-specific starter prompts in the taskpane.
- `FEATURE-006` has a Smart Auto routing slice: shared capability registry and runtime fields now classify add-in-only, companion-only, and either-runtime capabilities; taskpane-only mode remains usable; companion-only true viewport screenshots are hidden unless companion native capture is available; Office.js execution remains taskpane-owned; companion provider auth/inference remains capability-gated with no silent taskpane-secret migration.
- `FEATURE-006` also has a versioned Basic/Advanced taskpane-companion protocol slice: `TASKPANE_COMPANION_PROTOCOL` covers capability discovery, planned settings sync, reserved companion chat streaming, available companion tool requests, reserved Office tool result proxying, and planned explicit auth migration. Companion health/default taskpane fallback state advertises it through `CompanionCapabilities.protocol`, and docs/tests protect taskpane-owned Office.js execution plus the no-silent-secret-migration rule.
- `FEATURE-006` now has a user-visible runtime ownership preference: Settings -> Companion exposes Basic, Smart Auto, and Advanced; `UserPreferences.companionRuntimeMode` drives capability resolution, Basic hides companion-only tools from model turns even when a companion is connected, and Advanced uses taskpane fallback until companion agent/provider auth surfaces are available.
- `IMPROVEMENT-006` is closed: the Integrations MCP setup wizard clarifies scope vs companion vs transport, splits **Save connector** from **Check connection** with companion gating for stdio/remote HTTP, adds hosted MCP header fields and stdio passthrough through protocol/storage/companion bridge, and aligns `docs/privacy-and-storage.md` / `docs/capability-boundaries.md` with taskpane persistence vs companion execution.
- `IMPROVEMENT-007` is closed: the Integrations Library now uses compact connector list rows instead of card tiles, centers the connector search field, prevents long names like Supermemory from colliding with status pills, and uses icon-safe compact Diagnostics/audit buttons.
- `IMPROVEMENT-008` is closed: connector rows are denser, transport metadata now uses `HTTP` / `STDIO` chips, Library descriptions use available width, and the connector wizard Check step removes the generic host chip while coalescing duplicate local STDIO companion warnings.
- `FEATURE-007` is closed: connector catalog items now have curated setup profiles, companion-aware default setup paths, connected connector expansion with redacted config and tool inventory, companion-side full MCP tool classification, fail-closed execution targets, and warning-gated non-read tool enablement.
- `FEATURE-008` is closed: connector setup data now lives in validated YAML with generated typed TS, every setup profile carries provenance and availability metadata, Perplexity is official local STDIO/API-key, Obsidian/PostgreSQL Reader are community/reference local-only, Google Drive is planned/unverified, and browser-direct Streamable HTTP MCP/OAuth flows are supported for compatible hosted connectors. The catalog source is split into an ordered manifest at `addin/packages/pi-office-pack/src/connector-catalog.yaml` plus one connector YAML per connector under `addin/packages/pi-office-pack/src/connector-catalog/`.
- `FEATURE-004` now acts as the parent native Office editing epic for a Word-first API expansion split into open child tasks `FEATURE-009` through `FEATURE-018`, covering review annotations, formatting, lists, tables, sections, fields/citations, content controls, notes, search anchors, and desktop-gated layout objects.
- `IMPROVEMENT-003` and `IMPROVEMENT-009` are closed: Office tool metadata and schemas now live in host-specific registries under `addin/apps/taskpane/src/lib/office/tools/`, the in-process kernel consumes those registry definitions with host-support filtering, and the taskpane Office bridge is split into common, Word, Excel, and PowerPoint executors with drift tests covering protocol-to-registry-to-handler consistency. Large Excel and PowerPoint action executors are now thin dispatchers over focused domain modules for Excel range/worksheet/table/media/chart/pivot behavior and PowerPoint table/media/slide-structure/package/notes/chart behavior.
- `FEATURE-028`, `FEATURE-029`, and `FEATURE-030` are closed: Pi-Office now uses deferred Office/MCP discovery, a write-doc `office_tool_call` dispatcher for discovered Office tools, typed Office/MCP batch execution, and shared browser/companion MCP result handles so large tool schemas/results stay out of the baseline model context.
- `FEATURE-031` is closed and refined by `BUG-020`: sideload/localhost main chat can copy a redacted conversation debug JSONL containing visible chat, model-emitted reasoning deltas, raw bridge/session events, tool calls/results, Office state, session stats, and runtime diagnostics for shareable debugging.
- `BUG-019` is closed from the 2026-04-27 multi-agent implementation review: Word deferred bridge action types now align with implemented `applyWordAction` handlers, batch writes cannot rely on model-supplied approval flags, `word_collab_guard` is diagnostics-only, durable Word anchor metadata is preserved, and companion-routed MCP results use handles/pages/summaries.
- `BUG-016` is closed: companion discovery now uses a longer timeout, sanitized timeout/network/certificate guidance, localhost plus 127.0.0.1 fallback candidates, validated health responses, visible per-endpoint attempts in Settings, and clearer `3443` taskpane vs `3444` companion setup copy.
- `FEATURE-019` is closed: connector OAuth profiles can now declare YAML-driven broker metadata; Granola is companion-brokered with system-browser launch, loopback callback handling, DCR/PKCE/token exchange, companion-held token persistence, and companion-injected MCP bearer auth.
- `SECURITY-008` is closed for Windows companion hosts: companion-brokered OAuth tokens now persist through a token-store abstraction, Windows storage uses a DPAPI `CurrentUser` encrypted envelope with legacy JSON migration, taskpane connector cleanup/removal clears companion-held tokens when connected, and storage failure fails closed without leaving exchanged tokens usable in memory. `SECURITY-010` tracks non-Windows secure keychain backends and provider-side token revocation.
- `BUG-021` is closed: deferred Office schema fetch now accepts singular/plural `office_tool_get` arguments, `office_tool_search.maxResults` is honored, Word HTML insertion no longer silently falls back to literal text, unsafe HTML-shaped edit payloads fail with correction guidance, and prompt/tool guidance now steers generated Word documents through structured `insertHtml`, native page breaks, and verification before layout claims.
- `BUG-022` is closed: stale session model-sync failures no longer append repeated `Model change failed: Unknown session.` messages to the visible chat, while current-session model failures still surface.
- `BUG-023` is closed: `office_apply_edit.action` is now object-only in schema/guidance and JSON-string nested actions fail fast with a precise correction before legacy empty-text fallback.
- `BUG-024` is closed: Word document generation now supports explicit document-scope replace/append HTML contracts, ambiguous follow-up numbered sections without safe targets are rejected, and `verify_doc` warns on malformed numbered heading order/body-like Heading1 structure.
- `BUG-025` is closed: `office_tool_call.arguments` accepts JSON object strings, Word break types normalize to Office.js casing, `word_section_layout.insertBreak` returns OOXML-backed page-break evidence or fails when a requested page break is not persisted, and prompts forbid page-count claims without verification evidence.
- `BUG-026` and `BUG-027` are closed: repeated Word page-break insertion is idempotent unless `allowDuplicatePageBreak=true`, `verify_doc` flags empty heading artifacts and literal LaTeX, `office_apply_edit.insertHtml` rejects LaTeX/math payloads, and `word_equation` now provides native OfficeMath insertion with persisted `m:oMath` evidence.
- `BUG-028`, `BUG-029`, and `BUG-030` are closed: context usage now reports live estimated context separately from cumulative provider usage, visual tool results are compacted before model history/debug export bloat, `word_section_layout` accepts `anchor` as a target alias, document-end HTML append gets a paragraph boundary, and `verify_doc` flags glued/body-like heading paragraphs.
- `FEATURE-032` is closed: `word_equation` uses first-party LaTeX and Presentation MathML conversion into editable Word OfficeMath, covering common research-paper fractions, roots, scripts, Greek/operators, FFT-style sums, matrix/table forms, optional visible block-equation numbering/captions, persisted `m:oMath` verification, and fail-closed/warning metadata for unsupported input.
- `IMPROVEMENT-012` is closed: taskpane Office sessions explicitly use sequential native tool execution, while prompts distinguish ordered Office writes from parallel read/search work.
- `IMPROVEMENT-011` is closed: Settings now exposes a plain Drafting Style preference (`Balanced`, `Draft now`, `Ask me first`) that feeds mode-specific artifact drafting guidance into the runtime prompt without changing tool approval rules.
- `IMPROVEMENT-014` is closed: taskpane startup now keeps Settings/Integrations and the browser connector runtime behind lazy-load boundaries, skips connector runtime loading for empty-connector sessions, removes the eager `simple-icons` dependency, and preserves standalone connector fallback while reducing main taskpane JS from `1790.1 KiB` to `1526.8 KiB`.
- The broad independent taskpane transition is now committed: `a70cd9a` moves Pi session routes into the taskpane in-process kernel, reduces the companion to optional read-only file/MCP support, archives old companion source, and includes next-prompt suggestions, model curation, and runtime regression tests.
- Dev and sideload hardening is now committed as `a8a5a0d` plus `43b2a5b`, including the CI workflow, bundle budget, cert/port preflight, sideload resource preflight, manifest cache-bust version `1.0.0.2`, taskpane dev host on `https://localhost:3443`, and Vite cert loading scoped to the dev server only.

## Last worked on

- 2026-04-28: Advanced `FEATURE-006` with a persisted Basic/Smart Auto/Advanced runtime mode preference, Settings -> Companion mode switch, capability routing that hides companion-only tools in Basic mode, Advanced-mode taskpane fallback copy, docs updates, and regression coverage for runtime-mode validation/routing. Validation passed: `npm run typecheck:addin`, `npm --prefix addin run test:office -- protocol-parity capability-routing provider-auth-readiness model-curation` (257 tests), `npm run typecheck:companion`, `npm run build`, `npm run check:bundle`, `npm run validate:manifests`, and `git diff --check`.
- 2026-04-28: Advanced `FEATURE-006` with a shared versioned taskpane-companion protocol descriptor, companion health/default-state protocol advertising, `docs/advanced-companion-protocol.md`, README/capability-boundary links, and regression coverage for the Advanced-mode ownership seams. Validation passed: `npm run typecheck:addin`, `npm run typecheck:companion`, `npm run test:office` (254 tests), `npm run build`, `npm run check:bundle`, `npm run validate:manifests`, and `git diff --check`.
- 2026-04-28: Closed `IMPROVEMENT-010` by updating GitHub Actions CI to `actions/checkout@v6`, `actions/setup-node@v6`, and `node-version: 24` after confirming official action docs/release notes for the Node 24 runner transition; also removed the Node 24 `[DEP0190]` warning from the Office test harness by invoking Windows npm/npx commands without combining `shell: true` with an argument array. Local Node `v24.13.0` validation passed: clean add-in and companion installs, typecheck, build, bundle budget, manifest validation, Office tests, and whitespace gates.
- 2026-04-26: Created root `backlog.md`, added backlog stewardship and backlog-task commit discipline to `AGENTS.md`, and committed those documentation changes as `df51baf docs: add running backlog stewardship`.
- 2026-04-26: Added the standing rule that agents must automatically maintain `Project summary` and `Last worked on`, plus the fresh-session requirement to check `backlog.md` and correlate new work with existing backlog IDs.
- 2026-04-26: Recorded the expanded Pi-Office product goal in `AGENTS.md`, ran a multi-agent implementation review against local competitor/inspiration captures, and expanded `backlog.md` with the resulting security, provider, connector, visual-fidelity, and workflow tasks.
- 2026-04-26: Implemented the plan's first hardening pass: added originality/provenance and companion-mode direction, made permission timeouts fail closed, made `office_execute_js` manual-only, blocked false OAuth connected state, and marked remote HTTP connectors setup-only.
- 2026-04-26: Analyzed and grouped the dirty worktree into scoped commits: `2b5f058` for local factory ignore hygiene, `a8a5a0d` for BUG-001 dev/CI/preflight gates, `43b2a5b` for BUG-001 clean-checkout Vite cert loading, and `a70cd9a` for FEATURE-006 independent taskpane runtime work. Validation passed with `npm run typecheck`, `npm run build`, `npm run check:bundle`, `npm run validate:manifests`, `npm run test:office`, `npm run preflight:dev`, and a certs-absent `npm run build:taskpane` check.
- 2026-04-26: Closed `SECURITY-002` by adding protocol-level permission timeout coverage across write-doc, connector, read-external, and write-external categories plus session cleanup rejection coverage; `npm run test:office` passed with 89 tests.
- 2026-04-26: Closed `SECURITY-003` by enforcing one-time-only `office_execute_js` approvals in the runtime and permission popup, with protocol/UI tests for auto-approval, denial, attempted session approval, and blocked snippets; `npm run test:office` passed with 91 tests and `npm run typecheck:taskpane` passed.
- 2026-04-26: Closed `SECURITY-001` by requiring OAuth callback credential handoff before connected state, removing manual completion from connector UI, resetting tokenless imports/storage to `auth_required`, and adding callback contract tests; `npm run test:office` passed with 92 tests and `npm run typecheck` passed.
- 2026-04-26: Closed `SECURITY-005` by turning provenance notes into an audit matrix with evidence commands, adding contributor originality guidance, linking it from README, and replacing a copied-looking competitor reference in CSS; created `SECURITY-007` for third-party connector logo source/licensing review.
- 2026-04-26: Started `SECURITY-006` with a companion shell sandbox policy doc, README warning, and no-raw-shell regression coverage for active taskpane tools and companion routes; full sandbox backend/probes are still pending.
- 2026-04-26: Closed `SECURITY-006` by adding shared shell capability protocol, `CompanionShellSandbox`, capability/execute routes, taskpane `bash` gating behind available sandbox state, policy/destructive probe tests, environment scrubbing, output caps/timeouts, and a custom Pi `BashOperations` adapter; `npm run test:office` passed with 98 tests and companion/taskpane typechecks passed.
- 2026-04-26: Refactored the repository into independent `addin/`, `companion/`, `website/`, and `docs/` areas, keeping the root as a normal npm script router and preserving add-in/companion behavior with path-only config, CI, test, and documentation updates.
- 2026-04-26: Closed `BUG-010` by clearing the add-in npm audit findings, removing local vulnerable Office CLI packages from `npm ci`, pinning on-demand sideload/manifest CLIs, upgrading Vite to `8.0.10`, lazy-loading Mermaid, and verifying clean install/build/bundle/typecheck/manifest/test gates.
- 2026-04-26: Prepared a history cleanup for `.factory/` and `archive/companion/` by removing active test dependencies on `.factory/services.yaml`, ignoring future `archive/companion/` recreations, and removing both tracked trees before the rewrite.
- 2026-04-26: Closed `BUG-005` by adding provider auth states for stored/unverified/verified/failed credentials, updating Settings/model picker capability honesty, adding provider-readiness regression tests, refreshing stale local workspace package links, and validating with add-in typecheck, Office tests, add-in build, companion typecheck, bundle budget, and manifest validation.
- 2026-04-26: Closed `SECURITY-004` by adding the taskpane CSP meta policy, Privacy settings disclosure and clear-data controls, provider/connector clear-all runtime routes that remove local crypto keys, saved chat-history clearing, privacy/storage docs, and CSP/storage regression coverage; add-in typecheck, Office tests, add-in build, companion typecheck, bundle budget, and manifest validation passed.
- 2026-04-26: Closed `BUG-003` by tracing MCP stdio env behavior, adding explicit safe-env and credential injection for local stdio connectors, preserving manual local credential env targets from setup, and verifying companion/taskpane typechecks, Office tests, build, manifests, and bundle budget.
- 2026-04-26: Closed `BUG-008` by changing Excel checkpoint restore to write captured formula matrices, adding restore warnings for values-only fallback and unsupported workbook semantics, and validating add-in typecheck, Office tests, build, manifests, and bundle budget.
- 2026-04-26: Closed `BUG-007` by making snapshot and Excel range-image copy fidelity-honest, enforcing active-selection matching for `read_range_image`, updating the Claude parity tracker, and validating add-in typecheck, Office tests, build, manifests, and bundle budget.
- 2026-04-26: Closed `BUG-009` by resolving PowerPoint shape anchors through parent-slide metadata, narrowing the legacy slide-master tool to layout application only, updating PowerPoint guidance/tracker notes, and validating add-in typecheck, Office tests, build, manifests, and bundle budget.
- 2026-04-26: Obsoleted `SECURITY-007` after stakeholder clarified connector logos are acceptable as-is and do not need a licensing/source audit for the current release path.
- 2026-04-26: Advanced `FEATURE-002` and closed `BUG-006` by adding provider/auth capability metadata, `docs/provider-auth-matrix.md`, honest provider Settings cards, fail-closed browser auth routes for companion/OAuth providers, OpenAI-only image setup copy, image preference validation, and provider/image regression tests; add-in typecheck, Office tests, build, bundle budget, and manifest validation passed.
- 2026-04-26: Closed `FEATURE-003` by adding `OFFICE_WORKFLOW_PACKS`, host-specific workflow guidance and starter prompts for Word/Excel/PowerPoint professional artifacts, provenance notes, and regression coverage; add-in typecheck, Office tests, build, bundle budget, and manifest validation passed.
- 2026-04-26: Advanced `BUG-004` by adding latest-only Office state refresh tokens, stale-result/error suppression, repeated-error deduping, and policy tests; typecheck, Office tests, build, bundle budget, and manifest validation passed, with Word desktop smoke still pending.
- 2026-04-26: Advanced `FEATURE-002` and closed `IMPROVEMENT-005` by refreshing `pi-mono`, updating the taskpane Pi model catalog to `@mariozechner/pi-ai@0.70.2`, adding canonical provider/model curation YAML plus generated metadata, adding Simple/Advanced provider/model settings, user provider-default preferences, un-recommended model warnings/suppression, provider-auth matrix updates, and regression coverage; `npm run check:provider-models`, `npm run typecheck:addin`, `npm run test:office`, `npm run build`, `npm run check:bundle`, and `npm run validate:manifests` passed.
- 2026-04-26: Closed `BUG-004` from stakeholder Word desktop smoke and closed `BUG-011` by fixing companion loopback CORS, companion runtime diagnostics, connector-session resync after setup changes, and companion-routed remote HTTP MCP execution; companion/add-in typechecks, Office tests, build, bundle budget, and manifest validation passed.
- 2026-04-26: Advanced `FEATURE-006` with Smart Auto capability routing, extended companion capability metadata, session capability reporting, companion-only native viewport capture gating, Windows-first native capture API, Settings capability groups, and capability-boundary docs. Validation passed with add-in pack/taskpane typechecks, companion typecheck, Office tests, build, bundle budget, manifest validation, and diff whitespace check.
- 2026-04-26: Replaced Integrations connector inline side panel with a blurred modal overlay wizard: labeled step rail (back-only), footer CTAs (Continue / Check connection / Save connector), Esc/backdrop/close dismiss, connect-step validation, small inline Setup docs icons, and post-save wizard close to the Connected tab; `npm run typecheck:addin`, `npm run test:office`, `npm run build`, `npm run check:bundle`, and `npm run validate:manifests` passed.
- 2026-04-26: Closed `IMPROVEMENT-006` with the connector wizard redesign: companion-aware copy and `SettingsPage` wiring, scope disablement when the document is unsaved, custom vs library connection labeling, HTTP header and stdio passthrough forms, verify-step dual footer actions, finish-step save hint after verify-only success, companion-gated reverify, docs updates, and expanded `companion-connector-bridge` tests; validation via `npm run typecheck:addin`, `npm run typecheck:companion`, `npm run test:office`, `npm run build`, `npm run check:bundle`, and `npm run validate:manifests`.
- 2026-04-26: Closed `BUG-012` by adding dev-only browser preview fallback for direct `https://localhost:3443/` loads outside Office, hiding Office document tools in preview sessions, documenting host query options, and validating with browser screenshot/DOM checks, add-in typecheck, 144 Office tests, add-in build, bundle budget, and manifest validation.
- 2026-04-26: Addressed Integrations review feedback by reducing the read-only sources hero to a compact label, title, and single read-only safety chip.
- 2026-04-27: Closed `IMPROVEMENT-007` by converting the Integrations Library from cards to compact list rows, centering search, fixing the Supermemory/CUSTOM overlap, and replacing oversized Diagnostics/audit controls with compact icon-safe buttons; validation passed with add-in typecheck, 144 Office tests, add-in build, bundle budget, manifest validation, and browser screenshots.
- 2026-04-27: Closed `IMPROVEMENT-008` by tightening connector list row density, switching transport chips to `HTTP` / `STDIO`, removing the wizard Check host-list chip, and coalescing local STDIO companion prerequisites into one warning; validation passed with add-in typecheck, 144 Office tests, add-in build, bundle budget, manifest validation, in-app browser checks, and a 390px no-overflow pass.
- 2026-04-27: Closed `FEATURE-007` by adding curated setup profiles for the existing connector catalog, connected connector config/tool expansion, persisted per-tool policy overrides, non-read warning suppression, companion-side full tool inventory classification, and docs/provenance updates; validation passed with add-in/companion typechecks, 147 Office tests, build, bundle, manifest validation, and in-app browser screenshots.
- 2026-04-27: Closed `FEATURE-008` by moving connector catalog data to provenance-validated YAML/generated TS, adding browser-direct Streamable HTTP MCP verification/execution, implementing MCP OAuth discovery/DCR/PKCE/callback handling, correcting Granola/Perplexity/Obsidian/PostgreSQL/Google Drive setup honesty, and validating with catalog checks, add-in/companion typechecks, 148 Office tests, build, bundle, manifest validation, and in-app browser screenshots.
- 2026-04-27: Closed `BUG-013` by suppressing companion warnings for hosted HTTP setup profiles, moving OAuth window launch into the user-initiated UI click path with a dedicated named popup, keeping runtime OAuth URL generation side-effect-free, and validating with add-in typecheck, 148 Office tests, whitespace checks, build, manifest validation, and in-app browser inspection. Opened `BUG-014` because `check:bundle` now fails on the main taskpane chunk after the generated connector catalog work.
- 2026-04-27: Expanded `backlog.md` from the Word API audit by making `FEATURE-004` the parent epic and adding open Word-first child tasks `FEATURE-009` through `FEATURE-018`; validation was limited to `git diff --check` for the docs-only update and no commit was created.
- 2026-04-27: Closed `BUG-016` by hardening companion discovery with 5s attempts, localhost/127 fallback, sanitized timeout/network/certificate messages, visible attempt diagnostics, validated health responses, fast health capability reporting, and clearer `3443` taskpane versus `3444` companion setup copy. Validation passed with add-in and companion typechecks, 153 Office tests, build, manifest validation, and whitespace checks; `check:bundle` remains blocked by `BUG-014`.
- 2026-04-27: Closed `BUG-015` by keeping connector wizard drafts stable while companion diagnostics refresh in the background, treating hosted HTTP setup as taskpane-attemptable without companion warnings, marking Slack planned until Pi-Office has a registered Slack app path, adding clearer browser OAuth CORS failure guidance, and validating with connector catalog checks, add-in/companion typechecks, 150 Office tests, build, manifest validation, whitespace checks, and in-app browser QA. The then-failing bundle gate was later resolved by `BUG-014`.
- 2026-04-27: Closed `BUG-014` and `BUG-018` by raising the accepted main taskpane JS budget to 3 MB, refreshing Parallel Search MCP endpoints and OAuth metadata routing from first-party docs, fixing the wizard to save the active OAuth profile before starting sign-in, and opening `FEATURE-019` for a real connector OAuth broker/system-browser handoff. Validation passed with connector catalog check, add-in/companion typechecks, 151 Office tests, build, bundle budget, manifest validation, whitespace checks, and in-app browser Parallel OAuth smoke.
- 2026-04-27: Closed `FEATURE-019` by adding YAML-driven connector OAuth broker metadata, marking Granola companion-required with system-browser sign-in, implementing the companion OAuth broker for DCR/PKCE/callback/token exchange/token refresh, routing taskpane OAuth start through the companion when profiles request it, and adding regression coverage for brokered Granola setup and companion token injection.
- 2026-04-27: Closed `BUG-017` by adding companion OAuth status sync from the taskpane, preserving tokenless companion-brokered connected state across reloads, polling pending Integrations OAuth flows, and automatically re-verifying Granola after the companion callback completes.
- 2026-04-27: Synced a fresh branch from `origin/main` and closed `IMPROVEMENT-003` by extracting host-specific Office tool registries, splitting `office-bridge.ts` into common/Word/Excel/PowerPoint executors, adding registry/handler drift tests, restoring runtime-only `ask_user`/`generate_image` tool registration, and starting action-domain extraction for Excel formatting/table filters plus PowerPoint icons. Validation passed with add-in and companion typechecks, 158 Office tests, root build, bundle budget, manifest validation, and `git diff --check`.
- 2026-04-27: Closed `BUG-019` after the GPT-5.5 xhigh review by adding `office_tool_call`, making `office_batch_execute` write-doc, aligning deferred Word bridge actions with `applyWordAction`, preserving search/note/bookmark/hyperlink anchor metadata, removing protection mutation from `word_collab_guard`, and adding shared taskpane/companion MCP result-handle storage/routes. Validation passed with add-in and companion typechecks, 213 Office tests, root build, bundle budget, and manifest validation.
- 2026-04-27: Fixed the Vite dev resolver overlay for `@pi-office/pi-office-pack/mcp-result-store` by adding explicit browser/default export conditions for the shared MCP result store and regression coverage in `mcp-result-handles.test.ts`; `npm --prefix addin run test:office` passed with 214 tests and `npm --prefix addin run build:taskpane` passed.
- 2026-04-27: Fixed `Duplicate function definition provided: office_tool_call` by keeping runtime-registry tools out of the core Office registry publication path and adding duplicate-name regression coverage for the runtime agent tool inventory. Validation passed with 214 Office tests, taskpane build, and in-app browser reload with zero error/warn logs.
- 2026-04-27: Closed `FEATURE-031` by adding a sideload/localhost Diagnostics export for active conversation debug logs, capturing sanitized in-process agent state, model-emitted reasoning deltas, raw bridge/session events, tool calls/results, Office state, visible chat, and runtime diagnostics. Validation passed with add-in typecheck, 215 Office tests, add-in build, bundle budget, manifest validation, and diff whitespace check.
- 2026-04-27: Closed `BUG-020` by moving conversation debug sharing out of Settings and into the main chat header as an icon immediately before New chat, changing the action to copy complete sanitized JSONL directly to the clipboard with an Office-webview fallback, and updating docs/backlog. Validation passed with add-in typecheck, 215 Office tests, add-in build, bundle budget, manifest validation, and diff whitespace check.
- 2026-04-27: Closed `SECURITY-009` by adding a public non-commercial license, CODEOWNERS, repository-governance docs, and a GitHub `Protect main` ruleset via `gh api`; the ruleset requires PR/code-owner review for `main`, blocks force pushes/deletion, and uses repository-admin bypass because named-user bypass is unavailable for personal repositories.
- 2026-04-28: Closed `SECURITY-008` by adding a companion OAuth token-store abstraction, Windows DPAPI-backed encrypted token envelopes with legacy JSON migration, companion token clear routes wired into taskpane connector cleanup/removal, fail-closed persistence ordering, privacy docs, and regression coverage for encrypted storage, migration, clear, and storage failure.
- 2026-04-27: Closed `BUG-021` and `BUG-022` from `debugging-logs/1.tool-failures.json` by fixing plural `office_tool_get`, honoring `office_tool_search.maxResults`, making bare Word HTML route to `insertHtml`, rejecting unsafe HTML-as-text payloads, strengthening generated-document tool guidance, and suppressing stale-session model-change errors. Validation passed with `npm --prefix addin run test:office` (220 tests), `npm --prefix addin run typecheck`, `npm run build`, `npm run check:bundle`, `npm run validate:manifests`, and `git diff --check`.
- 2026-04-27: Closed `BUG-023`, `BUG-024`, and `BUG-025` from `debugging-logs/2.further-failures.json` by hardening Gemini Pro Word tool contracts: object-only `office_apply_edit.action`, document-scope Word replace/append HTML targeting, stale-section guardrails, structural `verify_doc` warnings, JSON-string `office_tool_call.arguments` parsing, and OOXML-evidenced page-break insertion. Validation passed with `npm --prefix addin run test:office` (226 tests), `npm --prefix addin run typecheck`, `npm run build`, `npm run check:bundle`, `npm run validate:manifests`, and `git diff --check`.
- 2026-04-27: Closed `BUG-026`, `BUG-027`, and `IMPROVEMENT-012`, advanced `FEATURE-032` and `IMPROVEMENT-011`, and hardened the Gemini Pro follow-up flow from `debugging-logs/3.not-perfect-gemini.json`: duplicate page breaks now no-op with evidence, literal LaTeX-in-HTML is rejected/flagged, `word_equation` inserts native OfficeMath with `m:oMath` verification, Office sessions use sequential tool execution, and prompts balance ask-user with fast-draft/creativity guidance. Validation passed with `npm --prefix addin run test:office` (233 tests), `npm --prefix addin run typecheck`, `npm run build`, `npm run check:bundle`, `npm run validate:manifests`, and `git diff --check`.
- 2026-04-27: Closed `BUG-028`, `BUG-029`, and `BUG-030` from `debugging-logs/4.Complete-Mess.json`: live context estimation no longer uses cumulative provider/cache usage, tool-result compaction strips raw viewport screenshots from model history, debug JSONL avoids duplicated kernel events, `word_section_layout.anchor` forwards correctly, document-end HTML appends get a boundary, and `verify_doc` warns on glued heading/body corruption. Validation passed with `npm --prefix addin run test:office` (235 tests), `npm --prefix addin run typecheck`, `npm run build`, `npm run check:bundle`, `npm run validate:manifests`, and `git diff --check`.
- 2026-04-28: Closed `IMPROVEMENT-013` by splitting the connector catalog into an ordered manifest plus 24 per-connector YAML files under `addin/packages/pi-office-pack/src/connector-catalog/`, updating the generator to validate manifest/file ID consistency, and preserving the generated runtime catalog except for the source comment. Validation passed with `npm --prefix addin run check:connector-catalog`, `npm run typecheck`, `npm run test:office` (235 tests), `npm run build`, `npm run check:bundle`, `npm run validate:manifests`, and `git diff --check`.
- 2026-04-28: Closed `IMPROVEMENT-014` by lazy-loading Settings/Integrations, dynamically loading the browser connector runtime only for connector routes or saved-connector sessions, removing `simple-icons`, and adding a startup loading-policy regression test. Validation passed with `npm run typecheck:addin`, `npm run build`, `npm run test:office` (236 tests), `npm run check:bundle` (`main.js=1526.8 KiB`), `npm run validate:manifests`, and `git diff --check`.
- 2026-04-28: Implemented the competitor-review-inspired Word Review Workbench workflow pack as an original Pi-Office workflow: it now appears first in Word starter prompts, routes review work through search/section reads/annotation/redline/proposal/verification tools, updates skill guidance, and is covered by workflow-pack regression tests.
- 2026-04-28: Advanced `IMPROVEMENT-009` by extracting Excel chart actions into `excel-actions/charts.ts` and PivotTable actions into `excel-actions/pivots.ts`, keeping `excel-actions.ts` as the dispatcher for those domains; `npm run typecheck:addin` passed.
- 2026-04-28: Closed `IMPROVEMENT-011` by adding the `artifactClarificationMode` preference, Settings Drafting Style controls for `Balanced` / `Draft now` / `Ask me first`, prompt injection for mode-specific artifact drafting behavior, runtime validation, and regression coverage. Validation passed with `npm run typecheck:addin` and `npm --prefix addin run test:office -- defaults-autonomy model-curation provider-auth-readiness` (238 tests).
- 2026-04-28: Advanced `FEATURE-032` by adding first-party LaTeX matrix environment conversion for Word OfficeMath, optional block-equation numbering/caption insertion, schema/prompt guidance, and regression coverage. Validation passed with add-in typecheck, Office tests (240 tests), root build, bundle budget, manifest validation, and whitespace checks.
- 2026-04-28: Closed `FEATURE-032` by adding first-party Presentation MathML input for `word_equation`, updating the bridge/schema/prompt guidance, preserving dependency/license decisions as no-new-dependency first-party conversion, and adding MathML regression coverage. Focused validation passed with add-in typecheck and `npm --prefix addin run test:office -- word-equation-tools` (248 tests); full build, bundle, manifest, and whitespace gates were run before commit.
- 2026-04-28: Advanced `IMPROVEMENT-009` by extracting PowerPoint table actions into `powerpoint-actions/tables.ts`, keeping `powerpoint-actions.ts` as the dispatcher for that domain, and adding a shared tool-contract regression for the table-domain route. Validation passed with add-in typecheck, Office tests (241 tests), root build, bundle budget, manifest validation, and whitespace checks.
- 2026-04-28: Advanced `IMPROVEMENT-009` by extracting PowerPoint media/icon/image actions into `powerpoint-actions/media.ts`, keeping `powerpoint-actions.ts` as the dispatcher for that domain, and adding a shared tool-contract regression for the media-domain route. Validation passed with add-in typecheck, Office tests (242 tests), root build, bundle budget, manifest validation, and whitespace checks.
- 2026-04-28: Advanced `IMPROVEMENT-009` by extracting PowerPoint slide-structure actions into `powerpoint-actions/slide-structure.ts`, reducing `powerpoint-actions.ts` to 666 lines, and adding a shared tool-contract regression for the slide-structure route. Validation passed with add-in typecheck, Office tests (243 tests), root build, bundle budget, manifest validation, and whitespace checks.
- 2026-04-28: Advanced `IMPROVEMENT-009` by extracting PowerPoint presentation package/theme, slide notes, and slide chart handlers into `powerpoint-actions/package.ts`, reducing `powerpoint-actions.ts` to 583 lines, and adding a shared tool-contract regression for the package/notes/chart route. Validation passed with add-in typecheck, Office tests (244 tests), root build, bundle budget, manifest validation, and whitespace checks.
- 2026-04-28: Closed `IMPROVEMENT-009` by extracting Excel range/value/formula/format/sort/filter/validation/conditional-format handlers into `excel-actions/ranges.ts`, worksheet lifecycle/visibility/print-area handlers into `excel-actions/worksheets.ts`, table create/style/filter handlers into `excel-actions/tables.ts`, and inline picture insertion into `excel-actions/media.ts`, reducing `excel-actions.ts` to a 70-line dispatcher. Validation passed with add-in typecheck, Office tests (245 tests), root build, bundle budget, manifest validation, and whitespace checks.

## Core rules

- Do not create Git commits unless the user explicitly asks, except when explicitly working a `backlog.md` task; backlog task work must be committed per the Backlog stewardship rules.
- Keep Git configuration repo-local only.
- Treat Pi as an external dependency, not vendored source.
- Keep host-side `Office.js` execution separate from Pi runtime and auth concerns.
- Do not restore old companion-era archive source into tracked history; use the top-level `companion/` package for active optional companion work.

## Backlog stewardship

- `backlog.md` is the canonical running backlog for bugs, features, improvements, security concerns, and testing gaps.
- Create backlog tasks automatically when you observe actionable gaps during reviews, investigations, plan-mode exits, implementation work, verification, or unrelated work that reveals a real issue.
- Update backlog tasks automatically when you explicitly work on them, prove them fixed, discover new evidence, split/merge scope, or realize a task is obsolete or no longer required.
- Do not delete backlog history. Mark tasks `done` or `obsolete` with evidence and keep enough context for a future agent with no chat history.
- Every backlog task must include category, status, priority, source, details, dependencies, subtasks, acceptance criteria, and notes/evidence.
- Keep task checkboxes unchecked unless the task status is `done` or `obsolete`.
- When working on a backlog task, commit that task's code/doc/test changes right away after validation, and keep the commit scoped to that backlog item only. Use conventional commit style and include the backlog ID in the subject, for example `fix(BUG-001): make taskpane build independent of local certs`.
- Backlog maintenance may be delegated to a subagent when the session explicitly allows delegation; prefer `gpt-5.3-codex` with high reasoning if model selection is available. The parent agent remains responsible for reviewing and integrating the backlog update.

## Architecture priorities

- Shared taskpane app for Word, Excel, and PowerPoint
- Independent local taskpane runtime without a live companion dependency
- Pi runtime as the primary AI core
- Internal Office Pi package for Office tools, prompts, skills, and tool gating
- Unsaved-document mode without local filesystem access
- Saved-document mode bound to the document folder for AGENTS/skills/file context

## Repo shape

- `addin/apps/taskpane`: Office-hosted React UI and Office bridge executor
- `addin/packages/pi-office-pack`: Office-specific Pi package
- `addin/manifests`: host-specific XML manifests
- `addin/scripts`: add-in validation and local setup helpers
- `companion`: optional local companion package for read-only file/MCP support and gated sandbox capability
- `website`: placeholder for the future public website
- `docs`: detailed project docs, trackers, provenance notes, and design references

## Quality bar

- Keep manifests valid and side-load friendly
- Prefer native Office edits over export/import hacks when possible
- Make bridge contracts explicit and versioned
- Validate build/typecheck/manifests before handoff
